# Changelog

All notable changes to this project are documented here.

The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and
this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Changed

- mcl_om `~> 0.28` with macula 12.2: the service answers `mcl-turn-credentials/info`
  with no code of its own, which also makes it count as online on the realm's
  Providers desk, and a failed publish announcement can no longer kill it.
- Built in `macula-ci-otp` and run on `macula-pq-runtime`, the team's image
  pair pinned by dated tag and digest, instead of the hexpm builder and a
  floating `alpine:3.22` runtime. The image is labelled with the commit it
  was built from.
- Built on mcl_om 0.27, which brings no barrel_docdb and so no rocksdb: the
  image and CI install no cmake and no rocksdb codec libraries.
- The boot claim is labelled for the realm's operator: `MCL_SERVICE_NAME`
  (`mcl-turn-credentials`) and `MCL_BOX`, the host that deploys it.
- A secret with characters outside latin-1 keys the HMAC with its UTF-8
  bytes, as coturn does, instead of failing every call.
- The health port is 8485, this service's own in macula-fleet `PORTS.md`,
  instead of the template default 8484, which is never assigned.

### Added

- The scaffold, generated by `rebar3 new mcl_service`: an OTP release that
  boots on `mcl_om`, joins the mesh, and answers `/health` on 8485.
- `mcl-turn-credentials/mint_credential`, ported from
  `hecate-turn-credentials`: one short-lived TURN credential in coturn's
  use-auth-secret scheme, open to any caller. Replaces
  `hecate_turn_credentials.mint_credential`.
- `/health` reports `down` while `TURN_SHARED_SECRET` is unset or empty.
- Unit tests asserting the contract's shape, the HMAC scheme coturn checks, and
  that the mesh-facing name and the reported version match the application's
  own.
- `rebar3 dialyzer` in CI, with macula in the PLT so the `macula_response`
  callbacks are checked.

### Changed from hecate-turn-credentials

- Every string in the reply is `{text, Bin}`. The hecate service sent bare
  binaries, which non-BEAM callers received as 0x-hex.
- A set-but-empty `TURN_SHARED_SECRET` is refused like an unset one, instead of
  minting credentials coturn rejects.
- `Containerfile` building an alpine image, with macula's QUIC NIF compiled from
  source rather than fetched against a foreign libc.
- `deploy/docker-compose.yml`, the service's own run contract.
- CI: `lint-and-test` on every push and pull request, `build-and-push` to
  the registry: `:latest` on `main`, the semver tag alone on `v*`.
